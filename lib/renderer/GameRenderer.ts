// Owner: A2. Frozen at end of M1.
// All template renderers (platformer/shooter/runner) MUST conform.

// TODO(A2): Define and export the GameRenderer interface:
//   - mount(container: HTMLElement, spec: GameSpec): void
//   - update(spec: GameSpec): void
//   - destroy(): void
//   - snapshot(): Promise<Blob>  // for thumbnails (used by A4)
export {};
